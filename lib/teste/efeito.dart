import 'package:flutter/material.dart';

class Efeito extends StatelessWidget {
  const Efeito({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
