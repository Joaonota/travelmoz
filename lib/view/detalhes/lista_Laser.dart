import 'package:flutter/material.dart';

class ListaLaser extends StatelessWidget {
  const ListaLaser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("OLa Mundo"),
      ),
    );
  }
}
